name: 'Build Packages v0.0.1'

on:
  push:
    tags:
      - '*_testing'

env:
  RELEASE_VERSION: ${GITHUB_REF#refs/*/}

jobs:

  build:
    name: Build EXE
    runs-on: windows-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v3
    # - name: Set up Python
    #   uses: actions/setup-python@v4
    #   with:
    #     python-version: '3.10'
    # - name: Install dependencies
    #   run: |
    #     python -m pip install --upgrade pip
    #     pip install -r .github/workflows/requirements.txt
    # - name: Build with Pyinstaller
    #   run: pyinstaller --clean --noconfirm .github/workflows/JWLManager.exe.spec
    - name: Create release
      id: gh_release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: ${{ env.RELEASE_VERSION }}
        name: ${{ env.RELEASE_VERSION }}
        draft: true
        prerelease: false
        body: |
          ____
          - Windows (zipped folder): JWLManager_${{ env.RELEASE_VERSION }}.zip
          - Windows (stand-alone): JWLManager_${{ env.RELEASE_VERSION }}.exe
          - macOS (zipped app): JWLManager_${{ env.RELEASE_VERSION }}.app.zip
    # - name: Upload to release
    #   id: upload-release-asset
    #   uses: actions/upload-release-asset@v1
    #   env:
    #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    #   with:
    #     upload_url: ${{ steps.gh_release.outputs.upload_url }}
    #     asset_path: ./dist/JWLManager.exe
    #     asset_name: JWLManager.exe
    #     asset_content_type: application/vnd.microsoft.portable-executable

