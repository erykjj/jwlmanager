name: 'Build Packages v0.0.1'

on:
  push:
    tags:
      - '*_testing'

jobs:

  build:
    name: Build EXE
    runs-on: windows-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v3
    # - name: Set up Python
    #   uses: actions/setup-python@v4
    #   with:
    #     python-version: '3.10'
    # - name: Install dependencies
    #   run: |
    #     python -m pip install --upgrade pip
    #     pip install -r .github/workflows/requirements.txt
    # - name: Build with Pyinstaller
    #   run: pyinstaller --clean --noconfirm .github/workflows/JWLManager.exe.spec
    - name: Create release
      id: gh_release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: ${{ github.ref_name }}
        name: ${{ github.ref_name }}
        draft: true
        prerelease: false
        body: |
          ____
          - Windows (zipped folder): JWLManager_$tag_name.zip
          - Windows (stand-alone): JWLManager_${ tag_name }.exe
          - macOS (zipped app): JWLManager_${{ tag_name }}.app.zip
    # - name: Upload to release
    #   id: upload-release-asset
    #   uses: actions/upload-release-asset@v1
    #   env:
    #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    #   with:
    #     upload_url: ${{ steps.gh_release.outputs.upload_url }}
    #     asset_path: ./dist/JWLManager.exe
    #     asset_name: JWLManager.exe
    #     asset_content_type: application/vnd.microsoft.portable-executable

